components:
  schemas:
    EventsListResponse:
      type: object
      description: >-
        Contains a list of events from all activated products
      additionalProperties: false
      required:
        - events
      properties:
        events:
          type: array
          items:
            $ref: 'EventsGetResponse.yaml'
        paginationKey:
          type: string