type: string
description: |
  Confidence level of the tampering detection.
  If a proxy is not detected, confidence is "high".
  If it's detected, can be "low", "medium", or "high".
x-ruleset-metadata:
  label: Tampering confidence
  category: browser
  enum-labels:
    low: Low
    medium: Medium
    high: High
