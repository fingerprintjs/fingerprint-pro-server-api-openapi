type: string
enum:
  - low
  - medium
  - high
description: >
  Confidence level of the proxy detection.
  If a proxy is not detected, confidence is "high".
  If it's detected, can be "low", "medium", or "high".
x-ruleset-metadata:
  label: Proxy confidence level
  category: ip_address
  enum-labels:
    low: Low
    medium: Medium
    high: High