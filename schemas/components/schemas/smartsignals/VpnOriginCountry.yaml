type: string
examples:
  - DE
description: |
  Country of the request (Android SDK version >= 2.4.0, iOS SDK version >= 2.9.0, JS agent >= 3.12.9 / 4.0.2), ISO 3166 format or unknown.
x-ruleset-metadata:
  label: VPN origin country
  category: ip_address
