---
'fingerprint-pro-server-api-openapi': minor
---

Add `event_id`, `timestamp`, `url`, `linked_id`, and `tags` to `EdgeResponse`; add `linked_id` and `tags` to `EdgeRequest`; add `source` query parameter to Events Search to filter Automation Intelligence (Edge) events
