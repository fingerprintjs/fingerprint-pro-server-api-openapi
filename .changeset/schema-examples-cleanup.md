---
'fingerprint-pro-server-api-openapi': patch
---

Add `examples` values across schema fields; normalize singular `example` to `examples`; add `required: []` on optional-property objects; add `unknown` to `ProxyDetails` type enum
